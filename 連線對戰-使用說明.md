# 連線對戰（同一 WiFi）使用說明

瀏覽器本身沒辦法像伺服器一樣「監聽」某個 IP 位址，讓另一台手機直接連進來——
這是瀏覽器的安全限制，不是這支遊戲的問題。所以要做到「一台選 Server、另一台
輸入 IP 就能連上」，需要另外跑一個很小的 Node.js 連線伺服器（`link-server.js`），
放在同一個 WiFi 網路內的某台裝置上（電腦、能跑 Termux 的手機都可以）。

## 檔案

- `index.html`：遊戲本體（已修改成 Server / Client 連線模式）
- `link-server.js`：小型連線中繼伺服器
- `package.json`：只依賴一個套件 `ws`

## 安裝與啟動（在「當 Server 的那台裝置」上執行一次）

1. 把 `index.html`、`link-server.js`、`package.json` 放在同一個資料夾。
2. 安裝相依套件：
   ```
   npm install
   ```
3. 啟動伺服器：
   ```
   npm start
   ```
   （或 `node link-server.js`；預設監聽 8787 埠，也可以用
   `node link-server.js 9000` 改用別的埠）
4. 啟動後畫面會列出這台裝置在區網內的 IP 位址，例如：
   ```
   http://192.168.1.23:8787/
   ```

## 兩台手機連線

1. **兩台手機都要連上同一個 WiFi。**
2. **當 Server 的那台**：用瀏覽器打開 `http://<這台裝置的IP>:8787/`
   （由跑 `link-server.js` 的那台裝置提供），在「連線對戰」設定裡
   選擇「**Server（伺服器）**」，按下「啟動 Server 連線」。
3. **當 Client 的那台**：打開同一份遊戲（可以是同一個網址，也可以是
   另外下載、單獨開啟的 `index.html`），選擇「**Client（客戶端）**」，
   輸入上面那個 IP 位址（連線埠預設 8787），按下「連線到 Server」。
4. 兩邊狀態燈都變綠、顯示「連線成功」後，就可以開始連線對戰／交換了。

## 小提醒

- 資料只會在同一個區域網路內傳輸，`link-server.js` 不會把任何東西送到
  網際網路上。
- 如果連不上，先確認：兩台裝置真的在同一個 WiFi、`link-server.js`
  有在跑、IP 位址和連接埠有輸入正確、手機/電腦防火牆沒有擋掉該連接埠。
- 執行 `link-server.js` 的裝置也可以是筆電、桌機，不一定要用手機本身
  （多數手機瀏覽器目前無法直接執行 Node.js，需要類似 Termux 的環境）。
